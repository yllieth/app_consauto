<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory android:title="@string/pref_header_defaults">
        <ListPreference
            android:key="liste_carburants"
            android:title="@string/pref_defaults_carburant_titre"
            android:defaultValue="-1"
            android:entries="@array/liste_carburants"
            android:entryValues="@array/liste_carburants"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null" />
    </PreferenceCategory>

</PreferenceScreen>